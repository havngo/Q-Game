// import { ALL_TILES } from "../../Common/Tests/test-utils";
// import { Coordinate } from "../../Common/coordinate";
// import { Referee } from "../../Common/referee";
// import { Color, Shape, Tile } from "../../Common/tile";
// import { Dag } from "../strategy";

describe("strategy tests", () => {
    test("dag works as intended", () => {
        // const { redCircle, redSquare, red8star, purple8star, orangeClover, greenClover } =
        //     ALL_TILES;
        // const ref = new Referee(["1"], redCircle, 3737);
        // const dag = new Dag(ref);
        // const hand = [redSquare, red8star, purple8star, orangeClover, greenClover];
        // expect(dag.getPlacements(hand)).toEqual([
        //     { coordinate: new Coordinate(0, 1), tile: new RegularTile(Color.RED, Shape.EIGHT_STAR) },
        //     { coordinate: new Coordinate(0, 2), tile: new RegularTile(Color.RED, Shape.SQUARE) },
        // ]);
    });
});
